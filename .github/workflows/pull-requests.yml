name: "Pull Request Linting"

on: [pull_request]

jobs:
  eslint:
    name: eslint
    runs-on: ubuntu-latest
    steps:
      - name: Run eslint
        run: |
          npm ci
          npm run lint
