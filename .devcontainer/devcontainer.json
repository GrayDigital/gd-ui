{
  "name": "Node.js & TypeScript",
  "runArgs": ["--init"],
  "build": {
    "dockerfile": "Dockerfile",
    "args": {
      "VARIANT": "16-bullseye"
    }
  },

  "settings": {},

  "extensions": ["trunk.io", "github.copilot"],

  "postCreateCommand": "npm i -g npm@latest && npm i",
  "remoteUser": "node",
  "features": {
    "github-cli": "latest"
  }
}
